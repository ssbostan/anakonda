apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  labels:
    app.kubernetes.io/name: mysql
    app.kubernetes.io/part-of: anakonda
    app.kubernetes.io/project: anakonda
    app.kubernetes.io/env: test
stringData:
  MYSQL_ROOT_PASSWORD: root
  MYSQL_DATABASE: anakonda
  MYSQL_USER: anakonda
  MYSQL_PASSWORD: anakonda
